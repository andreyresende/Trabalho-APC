Compilação simples:
gcc 180062433_01.c -o 180062433_01

Não foram implementados extras.
O código está comentado, ainda assim recomendo a correção com base na ordem com que as funções são chamadas, e não na ordem em que aparecem no código.
Para que tudo funcione os arquivos .txt e .bin sao necessários na pasta(já inicializados), o jogo por si só não criará os arquivos.
Boa correção =D.
